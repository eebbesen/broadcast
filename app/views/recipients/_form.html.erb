<%= form_with(model: recipient) do |form| %>
  <% if recipient.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipient.errors.count, "error") %> prohibited this recipient from being saved:</h2>

      <ul>
        <% recipient.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="py-4">
    <%= form.label :phone, style: 'display: block', class: 'font-bold' %>
    <%= form.text_field :phone, class: 'w-1/5' %>
  </div>

  <div class="mt-8 nine-grid">
    <%= form.submit 'Save', class: 'blue-button col-start-1 mr-5' %>
  </div>
<% end %>
